use crate::node_tree::NodeTree;
use crate::progress_mode::State;
use crate::identity::ParticipantSet;

pub fn enter(node_tree: &mut NodeTree, participant_set: &ParticipantSet) -> State {
    todo!()
}